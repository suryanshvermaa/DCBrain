# Changelog

Only documents changes to planning and documentation.

## [Unreleased]

### Changed
- **Task 017 — Notifications & Audit completed.** Implemented real-time notifications engine and audit logging. Defined Prisma `NotificationPreference` model and modified `Notification` model with `link` column (applied migration `20260716044606_add_notification_preferences_and_link`). Built Express `auditMiddleware` to automatically record write operations, logins/logouts, and specific pipeline routes (search, compliance, imports, agent runs) avoiding duplicates. Added admin audit log endpoint `GET /api/v1/admin/audit-log` (restricted to ADMIN role) and project-scoped activity query `GET /api/v1/projects/:id/activity`. Implemented native WebSocket server inside `websocket.ts` with user-to-socket registry mapping (supports multi-tab clients). Upgraded `base.agent.ts` and document processing worker (`worker.ts`) to trigger WebSocket-enabled notifications. Developed frontend components (`NotificationBell` dropdown with real-time listeners and chime sound) and routes (`/settings` for notification preferences, `/activity` for vertical timelines, and `/admin/audit-log` for administrative table filtering and JSON details modal).
- **Task 015 — Reporting Engine completed.** Implemented `reports` module at `backend/src/modules/reports/` with template engine (6 report types: daily, weekly, executive, compliance, risk, procurement), section aggregators calling all existing module services, Gemini AI summaries per section, PDFKit PDF generation, MinIO storage, REST API (`POST /reports/generate`, `GET /reports`, `GET /reports/:reportId`, `GET /reports/:reportId/download`), BullMQ `report-generation` worker, Prisma `Report` model with `ReportType`/`ReportStatus` enums (migration `20260715100000_add_reports`), upgraded `ReportingAgent` to call actual report generation, frontend /reports page with generate dialog, reports table, preview modal, and PDF/Markdown download. Tests: 13/13 passing (templates, PDF, schemas). Manual UI verification completed: fixed MinIO signature mismatch by proxying PDF stream through backend API, and fixed PDFKit blank pagination bugs.
- **Simulations Module:** Completed Task 016: Simulation Engine implementation, including manual verification of Neo4j graph traversal for downstream cascading impacts and Gemini-powered Mitigation Planner agent.
- **Bugfix (Schedule Dashboard):** Addressed the "Files Mixing" issue where historical schedule uploads were concatenated in the dashboard. `getScheduleActivities` now strictly returns activities from the most recent `SUCCESS` import.
- **Bugfix (Next.js Caching):** Cleared persistent `.next` caching issues within the Docker container that caused "Module not found" and obsolete UI chunks to be served.
- **Bugfix (Neo4j Sync):** Integrated Neo4j graph creation logic directly into the Postgres `scheduleActivity` transaction to ensure nodes/edges are actually synced for cascade queries.
- **Bugfix (Neo4j Constraints):** Dropped the flawed global `activity_name_unique` constraint from Neo4j (via Cypher shell and `neo4j.ts`) so users can upload updated schedules without triggering duplication errors.
- **Bugfix (API Routing):** Corrected `simulations/routes.ts` which incorrectly looked for `req.params.projectId` instead of `req.params.id`.
- **UI Improvements:** Integrated `@tailwindcss/typography` to correctly render AI Markdown mitigations. Removed legacy 'Cost Per Day' inputs from the Simulation creation form and transitioned Target Activity to a fully populated dropdown.
- **Task 014 — AI Agent Framework completed.** Implemented full agent module at `backend/src/modules/agents/` with `BaseAgentImpl` contract, Supervisor intent routing (Gemini JSON mode), all 14 sub-agents, REST API (`GET/POST /agents`, run history, schedule config), BullMQ `agent-execution` worker, `agent_runs`/`agent_schedules` Prisma models with migration `20260713100000_add_agent_models`, auto-triggers on document processing/schedule import/procurement import, in-app notification fan-out for findings, frontend `/agents` page with supervisor query and run history UI. Tests: agent module 9/9 passing.
- **Task 013 — Knowledge Graph completed.** Neo4j constraints, entity/relationship extraction in document pipeline, graph API at `/api/v1/projects/:id/graph`, React Flow frontend at `/graph`.
- **Task 010 — Deployment completed.** Created production deployment infrastructure: `.github/workflows/ci.yml` (lint + type-check + test on every push/PR; postgres/redis service containers), `.github/workflows/cd.yml` (build + push to GHCR, auto-deploy to staging on `develop`, manual-approval deploy to production on `main`). Updated `backend/Dockerfile` to 3-stage multi-stage build (deps→builder→runner, non-root `node` user, HEALTHCHECK). Updated `frontend/Dockerfile` (non-root `nextjs` user, HEALTHCHECK, build-arg NEXT_PUBLIC_ injection). Created `docker-compose.prod.yml` (image-based, Redis password auth, Nginx service, no source-code mounts). Created `nginx/nginx.conf` (HTTP→HTTPS redirect, TLS 1.2/1.3, OCSP stapling, HSTS/CSP/X-Frame security headers, 3 rate-limit zones: general/api/auth). Created `scripts/backup-db.sh` (pg_dump + gzip + 30-day rotation). Created `.env.prod.example` with all production overrides. Created `.ai/RUNBOOK.md` (first-deploy, manual deploy, rollback, backup restore, SSL renewal, GitHub Actions secrets). Updated `.gitignore` to exclude `.env.prod`, `backups/`, `nginx/certs/`. Tests: **9 suites / 24 tests — all passing**.
- **Task 009 — Dashboard completed.** Dashboard service (`backend/src/modules/dashboard/service.ts`) aggregates data from Document, ComplianceCheck, ScheduleActivity, and Activity tables. Computes composite health score (40% doc health + 30% compliance + 30% schedule). Redis cache with 5-minute TTL; `?refresh=true` bypass. Frontend main page (`app/page.tsx`) includes sidebar navigation, SVG health gauge, 4 stat cards with trend indicators, document processing breakdown, compliance score meter, schedule risk SPI meter, and activity feed with relative timestamps. Created dashboard routes test (`routes.test.ts`) — 3 tests. Full suite: **9 suites / 24 tests passing**.
- **Task 008 — Schedule Risk Prediction completed.** Added `ScheduleActivity` and `ScheduleImport` Prisma models with migration `20260713065346_add_schedule_activities`. Implemented P6 XML parser (`fast-xml-parser`), deterministic risk scoring engine (float consumption, critical path, duration, overdue), Gemini LLM mitigation generator with fallback, and 4 REST endpoints at `/api/v1/projects/:id/schedule`. Frontend schedule page includes Recharts scatter heat map, filterable activities table with expandable mitigation rows, health indicator cards (SPI, predicted completion, float rate), and drag-and-drop XML import. All 8 test suites pass.
- **Task 007 — Compliance Engine completed (initial).** Added `ComplianceCheck` Prisma model with migration. Backend compliance module with service, routes, and schemas. Frontend compliance page with run-check action and summary view. AI-powered evidence extraction and PDF export remain for future iteration.
- **Task 006 — Chat Interface completed.** Built a responsive Next.js Chat UI with distinct RAG source cards, suggested question pills, and centered premium styling. Backend uses LangGraph for an autonomous ReAct AI agent powered by Gemini. Added PDF export functionality.
- **Task 005 — RAG Search completed.** Built the backend retrieval pipeline using ChromaDB for semantic search and PostgreSQL for metadata filtering. AI agents successfully invoke `project_document_search` to cite sources.
- **Task 003 — Document Upload completed.** Implemented document management API, storing files in MinIO and metadata in Postgres.
- **Task 004 — Document Processing completed.** Implemented text extraction (PDF, DOCX, XLSX, Image OCR), semantic chunking with page number tracking, local batched embeddings via Xenova/bge-m3, duplicate detection against ChromaDB, and Gemini-based Entity Extraction storing nodes directly in Neo4j.
- **Frontend/backend auth routes synchronized.** Canonical auth requests now use `/api/v1/auth/*` everywhere, and project memory/docs were updated so future sessions keep the `/api` prefix.
- **Task 002 — Authentication completed.** Implemented JWT auth for `/api/v1/auth/register`, `/api/v1/auth/login`, `/api/v1/auth/refresh`, and `/api/v1/auth/me`; added bcrypt password hashing, RBAC middleware, refresh-token rotation, Redis-backed auth rate limiting, audit logging, frontend login/register pages, Redux auth state, and a protected dashboard wrapper. Added backend auth tests and frontend page validation.
- **Resolved Graph DB vendor decision (Neo4j 5.x).** `TECH_STACK.md` already listed Neo4j; `CURRENT_STATE.md`, `KNOWN_ISSUES.md` (ISSUE-004), `DATABASE.md`, `ARCHITECTURE.md`, and `DECISIONS.md` (ADR-010) updated to be consistent. ISSUE-004 marked Resolved.
- **Expanded `AGENTS.md`.** Every one of the 14 agents now documents Purpose, Responsibilities, Inputs, Outputs, Dependencies, Prompt Summary, and Future Improvements. Added an Agent Roster & Sprint Allocation table at the bottom.
- **`CURRENT_STATE.md` clarified.** Phase wording harmonised with `NEXT_CHAT.md` and `ROADMAP.md` — planning is complete; implementation is starting in Phase 1.
- **`REQUIREMENTS.md` FR-002 broadened.** Image uploads (PNG / JPG / TIFF) explicitly listed as a supported format with OCR (Tesseract) processing. Aligns with `PROJECT_CHECKLIST.md` and `DATASETS.md`.
- **Alias files no longer empty.** `UI_UX.md`, `LESSONS.md`, `CODING_RULES.md`, and `GIT_WORKFLOW.md` now each carry a short pointer to their canonical document (`UI_GUIDELINES.md`, `LESSONS_LEARNED.md`, `CODING_STANDARDS.md`, `GIT_RULES.md` respectively) so every required filename in the `.ai/` index resolves with useful content.
- **Task backlog synchronized with documented scope.** Created Tasks 011–018 covering Procurement Intelligence, RFI Intelligence, Knowledge Graph & Entity Extraction, AI Agent Framework & Supervisor, Reporting Engine, Simulation & Mitigation Planner, Notifications/Audit/Activity Timeline, and Advanced EPC Intelligence (NCR, Inspection, Commissioning, Change Orders). Updated [tasks/backlog.md](./tasks/backlog.md), [tasks/sprint.md](./tasks/sprint.md), and [TASKS.md](./TASKS.md) with sprint allocations and dependency graph. Each new task includes `task.md`, `plan.md`, `progress.md`, and `review.md`.
- **Existing tasks aligned with current stack.** Task 001 updated to include Neo4j and MinIO in Docker Compose; Task 003 updated to include image upload (PNG/JPG/TIFF) and MinIO storage; Task 004 updated to include image OCR, Node.js `xlsx` parser, entity extraction for Neo4j, and duplicate detection.
- **Corrected implementation plans for Node.js/TypeScript stack.** Rewrote `tasks/001-project-setup/plan.md`, `tasks/002-authentication/plan.md`, `tasks/003-document-upload/plan.md`, `tasks/004-document-processing/plan.md`, `tasks/005-rag/plan.md`, `tasks/006-chat/plan.md`, `tasks/007-compliance-checker/plan.md`, `tasks/008-schedule-risk/plan.md`, and `tasks/009-dashboard/plan.md` to use `.ts` files, Next.js App Router, Redux Toolkit, MinIO, Neo4j, and the module-based backend structure instead of Python-style paths and dependencies.
- **Synchronized `.ai/state/` files.** Updated `current_task.json` (MinIO/Neo4j, canonical file references), `state.json` (added modules 011–018, updated open issue count), and `versions.json` (removed `@tanstack/react-query`, added `minio`, `neo4j-driver`, `docx`, `xlsx`, `tesseract.js`, `ws`, `tsx`).
- **Synchronized `.ai/memory/` files.** Updated `conversation_summary.md` and `architecture_history.md` to remove Vite/TanStack Query, add MinIO/Neo4j, and reflect 18 tasks and the 14-agent ensemble.
- **Synchronized `.ai/prompts/` files.** Updated `frontend.md`, `implement.md`, and `database.md` to match the documented stack and canonical filenames.
- **Created `.ai/prompts/agents/` directory.** Added `index.md` plus 14 agent prompt files (one per agent) with system prompts, runtime variables, and example output.
- **Updated `.ai/templates/` and `PROMPTS.md`.** Templates now reference canonical files; `PROMPTS.md` indexes both the agent prompt library and development prompts.
- **Canonicalized file references.** Updated `NEXT_CHAT.md`, `README.md`, `FEATURES.md`, `COMPONENTS.md`, `SECURITY.md`, task files 001/002/006/009, and prompts (`backend`, `review`, `refactor`, `implement`, `database`) to reference `UI_GUIDELINES.md` / `CODING_STANDARDS.md` instead of the alias names, while preserving the alias files for required-file-name completeness.

### Added
- **Task 001 — Project Setup completed.** Full Docker Compose development environment (frontend, backend, worker, PostgreSQL + pgvector, Redis, ChromaDB, MinIO, Neo4j). Backend Express app factory with `/health`, `/api`, `/openapi.json`, and `/docs` (Swagger UI). Prisma schema, initial migration, and smoke tests. Next.js dashboard placeholder with Tailwind v4 design tokens and primary color scale. Backend and frontend lint/format/test tooling wired up.

### Notes
- No problem statement PDF was present in the repository. Project context was reconstructed from the existing `.ai/` documentation and `README.md`. The `.ai/` documentation is therefore the authoritative source until a problem statement is added at the repo root.
- Re-structured Mermaid diagrams to enforce strict compatibility across all parsers (carried over from previous unreleased notes).
