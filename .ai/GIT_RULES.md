# Git Rules

- **Branch Strategy:** `main` (production), `dev` (staging), `feature/name` (development).
- **Commit Style:** Conventional Commits (`feat:`, `fix:`, `docs:`, `chore:`).
- **PR Process:** Required 1 approval, pass CI tests before merging to `dev`.
- **Release:** Semantic versioning, tagged releases from `main`.
